
# TaskVault – Posts Manager

A Flutter project for managing posts with search, filter, pagination, and CRUD operations.

## Features
- View posts with pagination
- Search by title
- Filter by even IDs
- Create, edit, delete posts (with undo)
- View post details with comments

   git clone https://github.com/your-username/task_vault.git
2. Install dependencies:
   ```bash
   flutter pub get
3. Run the app:
   flutter run
